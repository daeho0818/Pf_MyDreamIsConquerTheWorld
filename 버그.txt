returning...
알아낸 사실
- DrawLine Switch에서 배열의 값이 3일 때는 imgColor의 값이 잘 들어가 있는 듯 하다.
- 하지만 Array의 값이 1이거나 9일 때는 알파값이 0이 되는 것을 포함해 값이 이상해진다.
- 그렇다고 1이나 9일 때 targetPixel에 imgColor[y * WINSIZEX + x] 넣는 것을 대신해 
D3DCOLOR_RGBA(255, 255, 255, 255); 넣으면 하얀색 아주 잘 된다  ㅋㅋ
-> 배열 인덱스에 문제 X, imgColor에 문제 X
그럼 뭐가 문제야 진짜 제발

- 첫번째 확인된 것 : 
배열 인덱스가 1이거나 9일 경우 해당 인덱스에 해당하는 imgColor 값은  1, 0, 0, 1 (빨강, 그리는 선 색)임
해당 인덱스의 imgColor가 DrawTempLine에서 바꿔준 색깔값을 갖고 있는 것.

- 두번째 확인된 것 : 
똑같은 이미지를 새로 만들고 불러왔을 때도 같은 현상 발생

- 세번째 확인된 것 : 
이중 for문으로 화면 전체를 imgColor로 바꿔도 다른 건 그대로여도 선(인덱스 1, 9)은 그대로임

case 1:
D3DXCOLOR 구조체에선 0~255 값이지만 D3DCOLOR_RGBA는 0~1 값이라서..?
근데 인덱스가 3일 때는 잘 됨

case 2:
imgColor가 현재 배경 정보를 받아와서 빨간 선 색깔이 그대로 불려오는 것
이 작업은 Init()에서 진행되는데 그럴 수 있나...?
그리고 이게 맞다면 다른 인덱스에선 이러지 않는 이유...?

할 것
- 타이틀 씬 UI들 이동하는거 선형보간으로 재구현하기